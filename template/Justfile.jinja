default:
    @just --list

init:
    uv sync
    uv run pre-commit install

build:
    uv build

lint:
    uv run pylint src/ tests/

publish:
    uv publish

test:
    uv run pytest --cov=src --cov-report=term-missing

{% if with_tox %}
tox:
    uv run tox
{% endif %}
