from fastapi import FastAPI

from {{ project_package }}.api.v1 import status
from {{ project_package }}.core.config import settings

app = FastAPI(
    title='{{ project_name }}'
)

app.include_router(status.router, prefix=settings.API_V1_STR)

@app.get('/')
async def root():
    return {'message': 'It works!', 'code': 0}
